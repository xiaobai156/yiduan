# 杀段_修复版项目专属规则

本项目继承上级 `..\AGENTS.md`，上级统一规则优先。本文件只规定“杀段_修复版”在新增站点、失败站点修复和检测重复方面的专属数据契约、正式入口、配置、缓存、输出、解析特例及当前实现边界。

## 1. 项目范围与正式文件

生产项目根目录：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀段_解耦版`

当前正式文件：

- 活跃站点配置：`sites.json`
- 正式抓取程序：`duan_crawler.py`
- 日常单期 BAT：`爬虫-每天杀段.bat`
- 正式近10期缓存：`recent_10_cache.json`
- 慢站运行状态：`slow_sites.json`
- 缓存式跨站判重器：`period_repetition_checker.py`
- 跨站判重 BAT：`爬虫-跨站重复检测.bat`
- 名为“站内连续重复+跨站重复”的 BAT：`爬虫-检测站内连续重复+跨站重复.bat`
- 失败站点独立验证器：`failed_site_validator.py`
- 已禁用的旧实时指纹程序：`duan_duplicate_checker.py`
- 已禁用的旧实时指纹 BAT：`爬虫-每天杀段 - 检测重复.bat`

`动态接口ID修复_隔离版\` 是历史隔离副本，不是生产入口、正式配置、正式缓存或当前代码依据。新增、修复和判重不得把其中的文件覆盖回项目根目录，也不得把其中的测试结果当作当前正式程序已经通过。

## 2. 当前生产数据快照

以下是 2026-07-31 对正式文件的静态核验结果，只用于执行前判断现状；以后配置或缓存变化时必须重新统计，不能把本节数字永久写死到业务代码：

- `sites.json` 有 150 个活跃站点，其中 `top` 60 个、`bottom` 90 个。
- 32 个站点显式配置 `api_url`。
- 8 个站点显式配置 `retry` 和 `cache_bust`。
- `赛马会第一版`、`赛马会第二版`、`锦瑟安然` 还配置了 `confirm: true`，正式抓取要求二次访问结果与首次完全一致。
- `recent_10_cache.json` 标记生成时间为 `2026-07-30T18:54:26`，有 150 条站点记录，最新期均为 211期。
- 缓存中 137 站为 `ok`，13 站为 `audit`；13 站虽然各有10条数据，但期数不连续。
- 缓存内 `source_sites` 指向旧路径 `C:\Users\Administrator\Desktop\每天工具\数据系列\杀段_修复版\sites.json`，不是本项目当前路径。

因此，当前缓存不能在本目录通过 `period_repetition_checker.py` 的来源路径校验；即使绕过该校验，13 个不连续站点也不能支撑全站正式“不重复”结论。修复并重建有效缓存前，正式判重状态只能报告“未完成”。

## 3. 杀一段数据契约

普通站点每期只能成功返回一个标准值：

`1段`、`2段`、`3段`、`4段`、`5段`、`6段`、`7段`

必须同时满足：

- 期数、目标“杀一段”语义和段值来自同一权威文档、同一栏目、同一区块。
- 普通结果恰好一个段值；零个、两个以上、越界值或无法唯一确认均失败。
- 页面原始内容必须有经站点规则确认的数据关键词和结果状态，不能只凭括号中出现 `N段` 成功。
- 段位表、广告、推荐、开奖结果、历史说明和其他杀号栏目中的段值不能作为目标结果。
- 同站同一期出现两个不同合法段值必须失败。
- 成功值标准化为 `N段`；不得把号码、生肖、尾数、头数、波色或其他项目数据写入本项目。

项目当前有两个明确的显示特例：

- `澳门曾夫人`、`宝马` 允许经专属页面结构证明的重复数字段表示法归一化为一个段值。
- `三地主` 允许从同一期明确列出的六个不同合法段位中计算七段全集唯一缺失的一个段；原始六段、转换过程和最终缺失段都必须留证。不是恰好六个不同合法段位时必须失败。

当前 `三地主` 实现不符合上述门槛：输入不是六个不同合法段位时仍可能返回非空段值，而严格校验只检查结果是否非空。修复代码并补齐反例测试前，凡不是恰好六个不同合法段位的内容必须直接失败，禁止生成普通候选。

上述特例只属于绑定的站名、URL、栏目和解析规则，不能推广到普通站点。当前通用正则会对任意站点接受部分“五个相同数字+段”的形式，范围过宽；新增或修复不得依赖该通用行为，应将重复表示法收紧到显式站点配置或专属解析器。

## 4. 配置、身份与新增字段

`sites.json` 是当前活跃站点配置。现有加载器实际读取的字段只有：

- `name`
- `url`
- `pick`，兼容读取 `region`
- `retry`
- `cache_bust`
- `confirm`
- `api_url`

加载器会拒绝完全相同的站名、完全相同的 URL，以及路径以 `/topic/<ID>.html` 结尾的重复 topic ID；它不会完整检查规范化 URL、查询参数文章 ID、`read.php?tid=`、`view.php?id=`、`Article.Aspx`、动态文章 ID、栏目、字段或配置指纹。

新增或修改前必须另外完成：

- 对 URL 去除无业务意义的大小写、尾斜杠和跟踪参数后比较。
- 提取并比较 topic、tid、id、ListId+id、动态文章记录 ID。
- 比较站名、目标栏目、数据字段、`pick` 和专属解析身份。
- 检查是否属于已封存身份；当前项目没有可证明完整的正式封存配置，不能因 `sites.json` 中不存在就认定从未封存。

不能只往 `sites.json` 追加 `name`、`url`、`pick` 后宣称新增完成。现有通用配置没有 `section_marker`、`data_marker`、区块起止、解析器 ID、来源策略或窗口字段，加载器还会静默忽略未知字段。新增站点需要这些约束时，必须先在隔离实现中让正式加载器真正读取并校验相应字段，再配置站点和测试。

若 `sites.json` 不存在，当前 `load_sites_config()` 会自动写出代码内置的 115 个默认站点；正式配置实际有 150 个站点，自动生成会丢失35站。生产规则固定为：`sites.json` 缺失、损坏或数量异常时必须失败，禁止触发默认重建，禁止用代码内置列表覆盖正式配置。

配置新增、修改和正式加入必须使用锁、同目录临时文件和原子替换，并在提交前后校验站点数量、顺序、身份及缓存配置指纹。当前项目没有正式 onboarding 命令或配置仓库，人工直接追加 JSON 不等于完成新增。

## 5. 文档来源、栏目锚点与方向

本项目方向窗口固定为3组，代码常量为 `CANDIDATE_WINDOW_LIMIT = 3`。`top`/`bottom` 是目标候选选择的最高优先级硬边界：

- `top` 只允许同一权威目标区块靠前3个高可信有效候选组。
- `bottom` 只允许同一权威目标区块靠后3个高可信有效候选组。
- 候选必须先通过文档身份、实际栏目锚点、数据语义、期数、唯一段值和合法值校验，再进入方向窗口。
- 相同展示可以去重；同一期不同段值必须先作为冲突保留，不能因窗口截断、方向或取首尾消失。
- 指定期不在方向窗口内必须失败，即使其他文档、其他区块或页面其他位置存在该期。

当前 `duan_crawler.py` 尚不满足上述完整来源边界：

- 主页面、解码内容、脚本和同源 iframe 都被放入一个字符串列表。
- 候选位置使用 `document_index * 10000000 + 文本位置`，把文档收集顺序当成页面上下顺序；`bottom` 可能把后收集的 script 或 iframe 当成页面尾部。
- `Candidate` 只保留期数、段值、标题、摘要、分数、顺序和合成位置，没有文档 URL、来源角色、实际锚点、锚点位置、区块 ID 或区块起止。
- 普通站点的通用解析不强制候选所在文档命中站名或栏目锚点。
- 部分站点会在一个文档命中站名、相邻文档命中结果词后持续激活栏目状态，存在跨文档借锚点路径。
- 候选分组键只有“期数+段值”，冲突检查只检查已经进入方向窗口的候选，窗口外或未被收集的同期冲突不会被发现。

在这些缺口修复前，涉及 script、iframe、解码文档、多帖子或同页多栏目的新增和修复不能使用通用解析成功，必须使用能证明同一权威文档和同一区块的隔离专属解析；无法证明就失败。

## 6. 动态文章和当前专属行为

`/article/admin/<记录ID>` 与 `/article/manager/<记录ID>` 必须绑定 URL 中的记录 ID。现有动态接口校验已经要求：

- 接口唯一命中 URL 的目标记录 ID。
- 多个同 ID 记录、ID 字段冲突或目标 ID 缺失时失败。
- `authorNickname` 与配置站名一致。
- 标题含期数和目标数据关键词。
- 正文含目标数据关键词。
- 仅有一个 `mainarticle` 栏目对象。

仍须收紧的现状：

- 配置没有保存期望栏目名称，代码只验证有一个主栏目，没有验证该栏目就是本站配置的目标栏目。
- 动态接口元数据未形成候选级完整证据，正文候选无法反向证明自己的文档 URL、栏目和区块。
- `/article/admin/` 对“接口不是结构化 JSON”或“接口未返回文章记录”也会进入页面/浏览器路径，范围宽于上级只允许 404 或 HTTP 200 空壳的兜底条件。
- `/article/manager/` 接口发生非文章记录异常时会返回空文档而不是完整分类；不得把此结果伪装成普通“无数据”。
- 项目没有实现 `/article/lottery/` 的同 ID 专属流程。

动态站新增或修复必须把 `api_url`、URL记录 ID、响应记录 ID、作者、栏目、正文和候选证据绑定为一个不可拆分身份。非404网络、TLS、HTTP 500、非可信 JSON、ID冲突或正文不明确时禁止浏览器补成功。

当前代码还包含以下站点级行为，变更时必须保留其真实结构约束并补测试，不能把站名分支当作可复用通用规则：

- `寒来暑往`：标题期数与“寒来暑往/绝杀一段”共同确立目标链，并按连续向下期数截断。
- `会飞的猪`：目标规则只允许从含“绝杀一段”的 HTML `<p>` 行提取；当前源码在没有匹配 `<p>` 时会回退全文通用解析，存在抓取其他栏目的风险。新增或修复时未命中目标 `<p>` 必须失败，禁止用全文回退补成功。
- `及第成名`：存在把已证明的特定错写期号映射回目标期的专属修正规则；只能在标题权威期、上一期和错写期位置关系全部满足时使用。
- `逍遥浪子`：先从列表标题锁定同一期“逍遥浪子/绝杀一段/实力见证”详情，再抓取同源详情。
- `澳门招财猫`、`淋漓尽精` 有明确站名锚点别名。
- `雷锋` 的目标脚本区块使用 `26064c.com` 或 `26064d.com` 锚点；`赛马会第一版`、`赛马会第二版` 的目标脚本区块使用 `877730c.com` 或 `877730d.com` 锚点。别名只授权同一脚本区块内的候选，不能跨文档借锚点。
- `赛马会第一版`、`赛马会第二版`、`锦瑟安然` 要求二次抓取确认。

`SITE_SPECIFIC_PARSER_NAMES` 当前只是未被正式分派逻辑使用的常量；不能因站名列在其中就宣称已经启用专属解析。大量正式行为仍由 `site.name == ...` 或站名集合控制，后续新增应迁入配置或独立解析器并保留完整测试，禁止继续在核心流程堆站名条件。

## 7. 日常单期抓取

日常入口：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀段_解耦版\爬虫-每天杀段.bat`

BAT 要求输入期数和并发数，空并发默认8，实际命令为：

`duan_crawler.py -i <指定期数> --workers <并发数>`

`duan_crawler.py` 的相关参数现状：

- `-i/--issues` 默认 `125`，但日常 BAT 不允许期数留空。
- `--workers` 默认8。
- `--timeout` 默认20秒。
- `--max-recheck` 默认最多低速复查10个网络失败站，`-1` 才是全部复查。
- `--sites` 默认 `sites.json`。
- `--slow-sites` 默认 `slow_sites.json`。
- `--recent-cache` 默认 `recent_10_cache.json`。
- `--no-recent-cache` 禁止本次刷新正式近10期缓存。
- 默认校验证书；只有明确授权才能使用 `--allow-insecure`。

正式单期必须只接收一个正整数期号。成功站点写一行 `N段 站点名`，并在成功文件尾部追加段值排行；失败站点进入独立失败文件。失败不能因重试、慢站记忆或缓存中有旧值而转成成功。

`slow_sites.json` 是运行调度状态，不是业务结果或判重缓存。它当前按 URL 保存失败连击，且使用普通 `write_text()`，没有锁和原子替换；并发运行或隔离验证不得修改正式 `slow_sites.json`。

## 8. 多期模式边界

当前 `duan_crawler.py` 接受 `094-120`、逗号或空格分隔的多期输入，但会把所有期数写入一个区间命名的成功/失败文件；未传 `--no-recent-cache` 时还会刷新正式缓存。日常 BAT 也没有阻止用户输入期数范围。

因此当前项目没有符合上级规则的正式多期入口。执行约束为：

- 日常 BAT 只能输入单一期数。
- 直接向 `duan_crawler.py` 传多个期数或范围不得作为正式生产抓取。
- 即使加上 `--no-recent-cache`，当前合并输出也不等于“每期独立调用单期逻辑并生成各自文件”。
- 需要多期功能时，必须先建立隔离多期入口：逐期调用单期服务、每期独立输出、生成总失败汇总，并强制禁止正式缓存和正式慢站状态写入。

## 9. 正式输出

按当前源项目路径计算，正式程序实际输出为：

- 成功目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳`
- 失败目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败`
- 成功文件：`<期数>期-段.txt`
- 失败文件：`<期数>期-段-失败.txt`

`爬虫-每天杀段.bat` 结束提示显示的是 `..\数据统一归纳\...`，与代码实际目录和失败目录不一致；以代码解析后的绝对路径为当前事实。修复 BAT 提示时只能改显示，不得擅自改变生产输出契约。

成功、失败和正式缓存使用同目录临时文件后替换，具备基础原子替换；当前没有跨进程文件锁。正式配置和缓存写入前必须补锁，防止两个 BAT 同时运行互相覆盖。失败报告必须保留期数、站名、URL、方向和具体原因。

失败 TXT 的正式显示格式为：

`失败 站名 URL 方向: bottom 期数: 216 阶段: 指定期数校验 原因: 具体原因`

每个失败站点单独一段，站点之间空一行；文件末尾使用 `失败分类统计`，分类数量使用 `<分类> <数量>条`。输出层不再显示内部诊断字段 `位置` 和 `实际期数`，它们仍保留在运行时失败证据中。

## 10. 正式近10期缓存

正式缓存：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀段_解耦版\recent_10_cache.json`

结构为 `duan_recent_10_cache.v1`，窗口10期，最多向前搜索30期。每站记录当前包含：

- `index`
- `name`
- `url`
- `pick`
- `latest_period`
- `sequence`
- `period_count`
- `is_consecutive`
- `status`
- `notes`
- `error`
- `script_error_count`
- `article_records`

普通站点每期缓存值只能是一个 `N段`；`三地主`也只能缓存六段补集计算出的唯一结果。缓存中的期号必须连续，缺期不能越过后拼成10条。

当前缓存和写入逻辑存在以下正式门禁：

- 当前文件的 `source_sites` 是旧目录路径，必须在正式全站重建时改为当前 `sites.json` 的规范绝对路径。
- 13个 `audit` 站点期数不连续，必须真实补齐或保留失败，不能按“已有10条”放行。
- 缓存身份比较键只有 `name+url`，未绑定规范 URL/topic/文章ID、栏目、数据字段、方向规则和完整配置指纹。
- 单期写入必须保留本次实时判定的状态；指定期失败必须写成 `error`，不得用旧缓存或历史候选伪装为本期成功。
- 缓存更新使用同目录锁、临时文件和原子替换；并发锁等待或原子写入失败必须报告缓存更新未完成。

单期实时判定完成并写入成功/失败 TXT 后，才允许更新缓存。缓存只用于滚动保存近10期数据和新增站点重复检测；缓存读取、缓存冲突或缓存写入失败不得改写本轮实时成功/失败结果。缓存更新失败时必须明确报告“缓存更新未完成”，并保留已经生成的 TXT。

## 11. 正式重复检测

项目当前可读的正式算法位于：

`period_repetition_checker.py`

它只读取 `recent_10_cache.json`，按双方共同具体期号比较连续相同段值：

- 连续1至2期：不列为重复。
- 连续3至5期：疑似重复，人工审核。
- 连续6期及以上：重复拒收。

缺失期会打断连续段，不能跨缺期拼接。普通站点比较的是单一标准段值；任何多值、冲突值或无效缓存记录不得参加正式结论。

当前三个判重入口的真实状态：

- `duan_duplicate_checker.py` 启动后立即输出禁用信息并返回2；其后面的实时指纹代码不可达，禁止调用或恢复为正式依据。
- `爬虫-每天杀段 - 检测重复.bat` 也明确禁用并返回2。
- `爬虫-检测站内连续重复+跨站重复.bat` 传入了 `period_repetition_checker.py` 不支持的 `--consecutive 8`，而且程序本身没有实现站内连续重复检查；该 BAT 不能作为正式入口。
- `爬虫-跨站重复检测.bat` 目标是缓存式跨站检查，但文件中使用了 `%%~dp0`、`%%ISSUE%%`、`%%PY_CMD%%` 等双百分号文本，且当前缓存来源路径不匹配；未经修复和验证不能作为正式工作入口。

`period_repetition_checker.py` 还只校验 schema、窗口、最大搜索数、来源路径和站点数量，不校验站点身份顺序、配置指纹、每站 `pick`、栏目、段值合法性或缓存新鲜度；它会让有数据的 `audit` 站点继续参加比较。

最关键的是，当前判重器只能比较缓存内已有站点，不能接收“尚未加入配置的候选站”及其现场真实历史。因此它不能完成新增站点准入判重。正式新增判重必须在隔离工具中同时读取有效现有站缓存和候选站真实抓取结果，按具体期号套用同一阈值；修复上述缺口前只能报告“判重未完成”，不能报告“不重复”。

## 12. 新增站点执行清单

本项目新增站点必须按以下项目专属顺序执行，上级通用门禁仍全部有效：

1. 记录名称、URL、`top/bottom`、真实栏目锚点、目标“杀一段”数据词、区块边界、来源类型和指定验证期。
2. 静态检查正式配置中的同名、规范 URL、topic/tid/文章ID、栏目、字段和方向身份。
3. 用户说“开始”后，在隔离环境真实访问权威页面或同 ID API。
4. 收集目标区块全部高可信候选，验证唯一 `1段` 至 `7段`，再执行方向最近3组。
5. 至少命中有效缓存最新期或上一期；普通准入取得连续10期，历史不足仅按用户本次明确特例处理。
6. 候选站真实结果与有效正式缓存按相同期号执行跨站判重；缓存失效或候选抓取失败时停止。
7. 建立候选站专属配置/解析器以及正确期、相邻期、不存在期、错栏目、方向越界、跨文档借锚点、同期冲突测试。
8. 隔离测试通过后才原子写入 `sites.json`；新增前后站点数量只增加本次批准数量，原有150站身份和顺序不变。
9. 正式加入后运行该站完整单期流程，再核对成功/失败输出、缓存身份和缓存冲突。

由于当前没有合规 onboarding 工具、候选站判重入口和完整配置字段，未补齐这些能力前不得把手工修改 `sites.json` 视为新增完成。

## 13. 失败站点修复

当前独立验证入口为：

`failed_site_validator.py`

它的默认清单路径为项目根目录 `failed_site_cases.json`，但当前正式目录不存在该文件；运行默认命令会失败。即使提供隔离清单，当前独立专属解析只实现了 `寒来暑往`，其他站点都会报告“未配置独立验证解析”。此外，现有验证器的锚点和关键词检查会跨所有文档合并，不能证明候选自身文档和自身区块具备锚点。

因此修复必须遵守：

- 第一阶段在隔离目录创建本次站点清单和专属实验解析，不改正式 `duan_crawler.py`、`sites.json`、缓存、慢站状态或TXT。
- 不得用通用 `collect_candidates()` 单次成功代替独立验证。
- 验证目标期、相邻期和明确不存在期，并保存文档 URL、来源角色、实际锚点、区块边界、原始期数行、原始段值行和方向窗口。
- script、iframe、解码正文、API、页面和列表详情必须作为不同文档验证；不能全局借锚点。
- 第二阶段只有用户再次明确“开始正式更新”后，才迁移最小配置或专属解析。
- 正式更新后运行目标站完整单期、相关回归和全项目测试；核对成功、失败、缓存冲突及所有其他活跃站点无回归。

当前根目录没有可执行的 `test_*.py` 源文件；只有 `__pycache__` 中历史编译产物。编译缓存不能作为测试通过证据。`动态接口ID修复_隔离版\test_dynamic_article_id.py` 只属于历史隔离副本，也不能代替当前正式代码的完整测试。

## 14. 当前实现缺口与完成门禁

新增、修复或判重工作报告“完成”前，必须明确处理或阻断以下当前高风险路径：

- 不得把主DOM、script、iframe、解码内容的收集顺序解释成页面 `top/bottom`。
- 不得让文档A的站名/栏目锚点授权文档B的段值。
- 不得让无效候选或同一期重复展示挤占最近3组窗口。
- 不得只检查窗口内冲突而遗漏同一权威区块已收集到的同期不同段值。
- 不得对无栏目配置的普通站点仅凭“期数+绝杀一段格式”成功。
- 不得把站名硬编码集合或未使用的常量描述为已经配置化的专属解析器。
- 不得让 `会飞的猪` 在未命中目标“绝杀一段” `<p>` 时回退全文通用解析成功。
- 不得让 `三地主` 在原文不是恰好六个不同合法段位时生成任何候选。
- 不得在多期、独立验证、历史回抓或审计中更新正式缓存、正式 `slow_sites.json` 或正式TXT。
- 不得使用来源路径不匹配、期数不连续或身份指纹不完整的缓存输出“不重复”。
- 不得把已禁用实时指纹脚本、损坏的判重 BAT 或历史隔离副本作为正式依据。
- 不得在 `sites.json` 缺失时自动生成115站默认配置覆盖150站正式配置。

最低验收证据应包括：

- 本次站点身份和方向配置。
- 权威文档、实际栏目锚点与明确区块。
- 方向最近3个高可信有效候选及目标期位置。
- 同期唯一段值和原始证据。
- 正确、相邻、不存在、方向越界、跨文档和冲突测试。
- 有效近10期缓存及按具体期号的判重报告。
- 正式单期成功/失败TXT与缓存核对。
- 配置、缓存、慢站状态和正式输出运行前后哈希。
- 项目真实测试文件、编译和完整回归结果。

任何一项未验证，只能报告“待验证”或“未完成”，不能报告为已新增、已修复或不重复。

## GitHub 自动同步规则

- 本项目的 origin 绑定为 https://github.com/xiaobai156/yiduan.git。
- 每次本项目修改完成并通过必要验证后，只提交本轮实际改动并推送到 origin 当前分支。
- 先保留用户已有未提交改动；不得把既有改动、无关项目、凭据、临时文件或测试产物带入提交和推送。
- 推送前必须检查暂存区和提交内容；若无法安全隔离本轮改动或验证未通过，停止提交/推送并报告。
