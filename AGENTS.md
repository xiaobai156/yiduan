# 杀段_解耦版项目规则

本文件继承上级 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\AGENTS.md`。
上级规则优先；本文件只保留本项目的差异和执行约束。

## 1. 范围与授权

- 只处理用户当前点名的站点、URL、期数、方向和文件。
- 禁止未经授权跑全站、跨期补值、读取缓存代替实时抓取，或影响其他站点。
- 未明确“开始”前只审计和说明；“开始验证”只做隔离验证；“开始正式更新”才写正式配置、缓存或 TXT。
- 封存站点不抓取、不写入、不更新缓存；恢复或删除必须由用户点名授权。
- 保留用户已有未提交改动；不覆盖无关文件，不推送凭据、临时文件或测试产物。

## 2. 结果正确性

成功必须同时证明：

- 站点身份、规范化 URL、topic/article/接口记录 ID；
- 栏目、作者或其他专属锚点，以及同一权威文档和区块；
- 目标期数、字段语义、`top`/`bottom` 方向；
- 唯一合法段值、数量、原始顺序和必要转换证据。

无法证明、来源混杂、栏目不明、同期冲突、期数越界或解析不完整，均判失败。禁止只用 URL、站名、缓存旧值或页面上孤立的“几段”作为成功依据。

普通结果只能是一个 `N段`，`N` 为 1–7；号码、生肖、尾数、波色等其他字段不得写入本项目。

专属特例只能绑定明确的站名、URL、栏目和解析器：

- `澳门曾夫人`、`宝马`：仅在专属页面结构证明重复数字表示同一段时归一化；
- `三地主`：仅当同一区块恰有六个不同合法段位时，计算唯一缺失的第七段，并保留原始六段和转换证据。

特例不得推广到普通站点；专属解析失败不得回退通用解析。

## 3. 方向与区块

- 方向窗口固定为代码配置的 3 组；不得运行时扩大、缩小或改成取页面首尾。
- 先锁定权威文档、栏目和区块，再收集该区块候选，最后按配置的 `top` 或 `bottom` 位置取值。
- 指定期不在配置方向窗口内必须失败；不得用其他区块、其他文章或相邻期补成功。
- 在选定的方向位置内，同一期出现不同段值必须失败；相同展示值可去重。
- 同期冲突判断必须服从已配置的位置，不能让窗口外候选覆盖或改变目标位置结果。
- API、主 DOM、iframe、script、OCR、列表和文章详情是独立来源；没有项目级授权不得拼接或互相补值。

## 4. 配置与架构

- `sites.json` 是活跃站点配置；缺失、损坏或身份/数量异常时必须报错，禁止自动生成默认配置或覆盖正式配置。
- 新增或修复站点必须核对 topic、tid、id、ListId+id、动态文章记录 ID、栏目、字段、`pick` 和解析器身份。
- 只追加名称、URL、`pick` 不算完成新增。
- 动态文章必须绑定 URL 记录 ID、响应记录 ID、作者、栏目和正文；ID 缺失、冲突、非可信响应或正文不明时失败。
- 遵守架构：`CLI/BAT → 服务层 → 抓取层 → 解析器 → 统一校验器 → 输出/缓存`。
- 抓取层只取原文；解析器不联网、不写文件；统一校验器负责裁决；存储层负责锁、临时文件和原子替换。

## 5. 正式入口与输出

正式入口：`爬虫-每天杀段.bat`；定向重抓入口：`duan_app/retry_failed.py`，对应 BAT 为 `爬虫-重抓失败.bat`。

正式输出目录：

- 成功：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳`
- 失败：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败`
- 文件：`<期数>期-段.txt`、`<期数>期-段-失败.txt`

成功行使用 `N段 站点名`；失败记录至少包含站点、URL、方向、期数、失败阶段和具体原因。内部诊断字段可留在运行证据，但不得混入正式失败 TXT 显示。

正式文件使用临时文件加原子替换，并在同一事务边界内取得共享锁、写入 TXT 和缓存。写入失败必须报告“缓存更新未完成”，不得改判成功。

## 6. 定向重抓

重抓只读取指定期的失败 TXT 白名单：

1. 逐站真实验证，不运行全站；
2. 成功只追加该站到对应成功 TXT，保留原有成功内容、值和顺序；
3. 成功后只删除该站当期失败行并更新该站当期缓存；
4. 仍失败则保留并更新该站失败记录；
5. 其他站点、其他缓存、其他 TXT 和正式配置保持不变。

排行榜、统计行不是站点记录，不能被当作业务结果重新写入或重复计数。缓存缺失、目标站点缺失、来源/版本/配置指纹不匹配时，停止提交并报告，不得静默跳过。

## 7. 缓存与验收

- 正式缓存：`recent_10_cache.json`；写入格式为项目当前 v2，窗口 10 期。
- 缓存必须绑定项目目录、来源身份、栏目、字段、方向和配置指纹，并通过完整结构、窗口、站点数量、摘要和版本校验。
- 实时结果先独立校验，定稿后才更新缓存；缓存不得反向裁决实时结果。
- 代码、配置、缓存、存储或统一校验有改动时，运行相称的静态检查、Python 编译和受影响测试；站点修复必须做指定期真实验证。
- 未完成真实验证或正式入口验收，只能报告“未完成/待验证”。

## 8. GitHub 同步

- origin：`https://github.com/xiaobai156/yiduan.git`
- 每次本项目修改完成并通过必要验证后，只提交本轮实际改动并推送当前分支。
- 提交前检查分支、状态、暂存区、提交内容和远程状态；不得 broad-stage 生成物或既有改动。
- 无法安全隔离本轮改动，或验证未通过时，停止提交/推送并报告。
